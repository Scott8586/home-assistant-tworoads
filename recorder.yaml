purge_keep_days: 30
exclude:
  domains:
  - weblink
  - updater
  - media_player
  entities:
  - sensor.moon
  - sun.sun
  - sensor.last_boot # Comes from 'systemmonitor' sensor platform
  - sensor.time
  - sensor.erx_wan_in
  - sensor.erx_wan_out
